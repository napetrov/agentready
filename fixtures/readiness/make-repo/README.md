# Good Make Fixture

A minimal C project driven by a `Makefile` that exposes build, test, lint, and
typecheck targets.

Run validation with:

```bash
make build
make test
make lint
make typecheck
```
