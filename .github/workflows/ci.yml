name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test -- --runInBand
        env:
          OPENAI_API_KEY: test-openai-key-placeholder
          GITHUB_TOKEN: test-github-token

      - name: AgentReady self scan
        run: npm run agentready -- scan . --json --compact

      - name: AgentReady fixture smoke
        run: npm run agentready:fixtures

      - name: AgentReady PR regression gate
        if: github.event_name == 'pull_request'
        run: npm run agentready -- diff --base origin/${{ github.base_ref }} --head HEAD . --json --compact --fail-on-regression

      - name: AgentReady markdown report
        if: github.event_name == 'pull_request'
        run: npm run agentready -- diff --base origin/${{ github.base_ref }} --head HEAD . --markdown

      - name: Build
        run: npm run build
        env:
          OPENAI_API_KEY: test-openai-key-placeholder
          GITHUB_TOKEN: test-github-token

  security:
    name: Security Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Audit dependencies
        run: npm audit --audit-level=high
        continue-on-error: true
